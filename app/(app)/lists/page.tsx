/** Phase 3 §2.7 Lists (purchase lists) — owned by a later phase's UI even though the PurchaseList backend already exists (Phase 5). Calm, honest "coming soon." */
export default function ListsPage() {
  return (
    <div className="flex flex-col items-center gap-2 p-8 text-center">
      <h1 className="text-xl font-semibold">Λίστες</h1>
      <p className="text-zinc-600 dark:text-zinc-400">Έρχεται σύντομα.</p>
    </div>
  );
}
